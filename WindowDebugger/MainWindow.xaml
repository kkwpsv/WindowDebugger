<Window x:Class="WindowDebugger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowDebugger"
        xmlns:views="clr-namespace:WindowDebugger.Views"
        xmlns:viewmodels="clr-namespace:WindowDebugger.ViewModels"
        mc:Ignorable="d" DataContext="{x:Static viewmodels:ViewModel.Instance}"
        Title="WindowDebugger" Height="500" Width="800" MinHeight="400" MinWidth="600" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <ListBox x:Name="WindowList" Grid.Row="0" ItemsSource="{Binding Windows, Mode=OneWay}" SelectedItem="{Binding SelectedWindow, Mode=TwoWay}"/>
            <Button Grid.Row="1" Content="Refresh" Click="RefreshWindowList"/>
        </Grid>
        <TabControl Grid.Column="1" DataContext="{Binding SelectedWindow}">
            <views:WindowStylesTab Header="Styles"/>
            <views:WindowStylesExTab Header="StyleExes"/>
        </TabControl>
    </Grid>
</Window>
